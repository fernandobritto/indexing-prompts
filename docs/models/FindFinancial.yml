name: FindFinancialResponseBody
description: GET /financial expected request body
content:
  application/json:
    schema:
      type: object
      properties:
        Items:
          type: array
          items:
            - Financial:
              type: object
              description: Financial result object.
        LastEvaluatedKey:
          type: object
          description: Optional object, used to paginate results. If present on the response, it means there are more items to retrieve.
