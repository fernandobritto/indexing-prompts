name: SuccessResponseWithMessage
description: GET /health/gateway expected response.
content: 
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string